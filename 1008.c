#include <stdio.h>

int main (){
	float a, b, c;
	scanf ("%f %f %f", &a, &b, &c);
	float salario = (b*c);
	float number = a;
	printf ("NUMBER = %.0f\n", number);
	printf ("SALARY = U$ %.2f\n", salario);
return 0;
}
